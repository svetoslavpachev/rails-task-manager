<h1>Here are all of the tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task)%></li>
 <%= link_to "Edit this task", task_edit_path(task) %>
  <% end %>
</ul>

<%= link_to "Create new task", tasks_new_path %>
